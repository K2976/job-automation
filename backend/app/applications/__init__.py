"""V3 — Application Automation. A deterministic-first Playwright layer that fills and
(optionally) submits applications for READY_TO_APPLY opportunities. The intelligence lives
in the engine (field mapper, question engine, validation, approval); Playwright is only the
driver behind the BrowserPage protocol. See docs/application-automation.md."""
