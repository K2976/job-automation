# Base URL of the API backend. Build-time (Vite inlines it at `npm run build`).
# Empty = same origin (local dev via the Vite proxy, or the FastAPI-served build).
# On Vercel, set this to your Render backend URL, e.g. https://adaptive-resume-api.onrender.com
VITE_API_BASE=
