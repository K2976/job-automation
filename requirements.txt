# Core API
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
jinja2>=3.1

# Models & config
pydantic>=2.6
pydantic-settings>=2.2

# Retrieval math
numpy>=1.26

# Document extraction + PDF export (reportlab is pure-python, no system libs)
pypdf>=4.0
python-docx>=1.1
reportlab>=4.0

# LLM providers over REST (no vendor SDK lock-in)
httpx>=0.27

# V3 browser automation. The package is light; the browser binaries are NOT (~150MB) and
# are installed separately with `playwright install chromium` — only on the browser worker,
# never in the API web service (see docs/deployment.md). Importing the app never needs it.
playwright>=1.40

# Dev / tests
pytest>=8.0

# Optional, opt-in only (EMBEDDING_PROVIDER unaffected by default):
#   sentence-transformers  -> heavier local semantic embeddings (pulls torch)

# Professional-PDF export uses jinja2 (above) to render LaTeX. Compilation needs a LaTeX
# engine on PATH — NOT a pip package: install `tectonic` (recommended) or a TeX distro.
# Without one, .tex export still works and reportlab PDF is the fallback (docs/deployment.md).
